# LENAH — Gmail Draft MVP (Streamlit)

Smallest possible MVP:
- Streamlit UI
- Simple email template
- Creates a Gmail **DRAFT** (does not send)

### ) Install requirements
```bash
pip install -r requirements.txt
